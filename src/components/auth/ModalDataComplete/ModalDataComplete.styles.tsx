import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  modalView: {
    top: 0,
    width: "100%",
    height: "100%",
  },
  modalContent: {
    paddingHorizontal: 30,
    paddingTop: 40,
  },
})
